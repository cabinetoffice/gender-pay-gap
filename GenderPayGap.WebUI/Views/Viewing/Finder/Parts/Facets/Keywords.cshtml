@model GenderPayGap.WebUI.Models.Search.SearchViewModel
<div id="SearchByKeywordsGroup" class="form-group" style="position: relative;">

    <fieldset class="inline">
        <legend class="visually-hidden">Radiobutton selecting a search term</legend>
        <label for="FinderKeywords">
            <h1 class="heading-large">
                Search by <span class="visuallyhidden">“@(Model.search)”</span>
            </h1>
        </label>

        <div class="multiple-choice">
            <input id="rdoEmployerName" type="radio" name="t" value="1" @(!string.IsNullOrEmpty(Model.t) && Model.t == "1" ? "checked" : "")/>
            <label for="rdoEmployerName">Employer name</label>
        </div>
        <div class="multiple-choice">
            <input id="rdoBySectorType" type="radio" name="t" value="2" @(!string.IsNullOrEmpty(Model.t) && Model.t == "2" ? "checked" : "")/>
            <label for="rdoBySectorType">Employer type or SIC code</label>
        </div>

        <div class="panel panel-border-wide">
            <div class="font-xsmall">Search by employer or industry type, eg banks, supermarkets, law firms. You can also search by SIC codes which provide an easy way of describing what an employer does. <a href="https://resources.companieshouse.gov.uk/sic/" target="_blank">Find SIC codes</a></div>
        </div>

    </fieldset>

    <fieldset>
        <div class="gov-uk-c-searchbar">
            <div class="gov-uk-l-searchbar__table">
                <div class="gov-uk-l-searchbar__cell">
                    <input id="FinderKeywords"
                           value="@(Model.search)"
                           aria-expanded="false"
                           class="gov-uk-c-searchbar__input"
                           name="search"
                           type="text"
                           autocomplete="off"
                           spellcheck="false"
                           maxlength="100"/>
                </div>
                <div class="gov-uk-l-searchbar__cell">
                    <button class="gov-uk-c-searchbar__submit gov-uk-c-searchbar__submit--blue" id="NextStep" type="submit">Search</button>
                </div>

            </div>
        </div>
    </fieldset>

</div>